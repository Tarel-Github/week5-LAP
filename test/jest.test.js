// test('1+1은 2, 테스트용 코드', () =>{
//     expect(1+1).toEqual(2)
// })


// const Post = require('../controllers/posts.controller');
// post = new Post()
// const getPosts = post.getPosts

// const User = require('../controllers/Users.controller');
// user = new User()
// const login = user.login

// console.log(login)

// describe('login', () => {

    // const req = {
    //   body: {"nickname": "qqq", "password":"123123"}
    // };
    // const res ={
    //     status: jest.fn(()=>res),
    //     send: jest.fn(),
    // }
    // test('로그?인', async() =>{
        
    //     // await login(req,res);
    //     // expect(res.send).toBeCalledWith('Token이 정상적으로 발급되었습니다.')


    // })
    // const res = {
    //   status: jest.fn(() => res),
    //   send: jest.fn(),
    // };
    // const next = jest.fn();
  

//   });




// describe('post', () => {

//     const res = {
//       status: jest.fn(() => res),
//       send: jest.fn(),
//     };
//     test('게시글 함 보자', async() =>{
        
//         getPosts
//     })
    // const res = {
    //   status: jest.fn(() => res),
    //   send: jest.fn(),
    // };
    // const next = jest.fn();
  
    // test('로그인 되어있으면 isLoggedIn이 next를 호출해야 함', () => {
    //   const req = {
    //     isAuthenticated: jest.fn(() => true),
    //   };
    //   isLoggedIn(req, res, next);
    //   expect(next).toHaveBeenCalledTimes(1);
    // });
  
    // test('로그인 되어있지 않으면 isLoggedIn이 에러를 응답해야 함', () => {
    //   const req = {
    //     isAuthenticated: jest.fn(() => false),
    //   };
    //   isLoggedIn(req, res, next);
    //   expect(res.status).toBeCalledWith(403);
    //   expect(res.send).toBeCalledWith('로그인 필요');
    // });
  // });







// describe('isLoggedIn', () => {
//     // const res = {
//     //   status: jest.fn(() => res),
//     //   send: jest.fn(),
//     // };
//     // const next = jest.fn();
  
//     // test('로그인 되어있으면 isLoggedIn이 next를 호출해야 함', () => {
//     //   const req = {
//     //     isAuthenticated: jest.fn(() => true),
//     //   };
//     //   isLoggedIn(req, res, next);
//     //   expect(next).toHaveBeenCalledTimes(1);
//     // });
  
//     // test('로그인 되어있지 않으면 isLoggedIn이 에러를 응답해야 함', () => {
//     //   const req = {
//     //     isAuthenticated: jest.fn(() => false),
//     //   };
//     //   isLoggedIn(req, res, next);
//     //   expect(res.status).toBeCalledWith(403);
//     //   expect(res.send).toBeCalledWith('로그인 필요');
//     // });
//   });
